{
  "name": "symcon-mcp-server",
  "version": "1.3.51",
  "description": "MCP Server for IP-Symcon - exposes Symcon API as MCP tools",
  "author": "Niko Sinthern",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "build:exe": "bun build src/index.ts --compile --target=bun-windows-x64 --outfile symcon-mcp-server.exe"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
